<!--
  TESTGEN GUIDANCE FILE
  =====================
  Optional. Type any custom instructions for the test-generation agent BELOW this
  comment block, then commit the file. On the next run the agent treats whatever you
  write here as required coverage — e.g. specific edge cases, boundary values, status
  codes, or error messages it must assert.

  Leave it empty (only this comment) to give no special guidance — the agent then uses
  its own judgment.

  Examples of useful guidance:
    - Always test the exact boundary value on both sides (e.g. price 1000000 allowed,
      1000001 rejected with the exact message).
    - Cover an empty request body and a missing required field for every POST.
    - Assert the exact error message text, not just the status code.

  Note: the QA "regen-tests" label flow overwrites this file with the reviewer's
  comment for that single run; your committed instructions remain in git.
-->
