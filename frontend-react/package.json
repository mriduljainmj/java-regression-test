{
  "name": "frontend-react",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "React Product Catalog — a second frontend (different language/framework) proving the agent's UI test generation is framework-agnostic.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "node serve-dist.js",
    "test": "vite build && cucumber-js",
    "test:install": "playwright install chromium"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^11.2.0",
    "@vitejs/plugin-react": "^4.3.4",
    "playwright": "^1.49.0",
    "vite": "^5.4.11"
  }
}
